[package]
name = "pcb-drc"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Native design rule checker for the pcb tool."

[dependencies]
pcb-core = { path = "../pcb-core" }
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true
